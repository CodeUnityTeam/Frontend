// Primary export: contributor's visual/pill Tag
export { Tag } from "./tag";

// Expose our chip-style Tag (supports remove) for register UI as `registerTag`
export { Tag as registerTag } from "@/widgets/tags/ui/tag";

