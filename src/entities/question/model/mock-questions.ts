import type { Question } from "./types";

const mockQuestions: Question[] = [
  {
    id: "1",
    title:
      "Как собрать портфолио, если нет коммерческого опыта, но хочется попасть в реальные проекты?",
    details:
      "Я начинающий разработчик и пока не работал в коммерческих командах. Учебные проекты есть, но они выглядят слишком “учебными”, и я не уверен, что их стоит показывать.\n\nКак правильно собирать портфолио без реального опыта? Стоит ли участвовать в open-source или лучше искать командные pet-проекты? Хочется получить опыт, который будет полезен для будущего трудоустройства и не тратить время впустую.",
    tags: ["design", "figma"],
  },
];

export function getQuestionById(id: string): Question | undefined {
  return mockQuestions.find((question) => question.id === id);
}
